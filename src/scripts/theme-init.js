// Storage may be unavailable; the system preference still supplies the initial theme.
(function(h){var s;try{s=localStorage.getItem('theme')}catch{};h.dataset.theme=(s?s==='dark':matchMedia('(prefers-color-scheme: dark)').matches)?'dark':'light';if(!matchMedia('(prefers-reduced-motion: reduce)').matches&&!location.hash&&!scrollX&&!scrollY&&performance.getEntriesByType('navigation')[0]?.type!=='back_forward')h.classList.add('px-wait');setTimeout(function(){h.classList.remove('px-wait')},3000)})(document.documentElement)
